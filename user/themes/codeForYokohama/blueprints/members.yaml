title: Blog
'@extends':
    type: default
    context: blueprints://pages

header.content.pagination:
      type: toggle
      label: Pagination
      highlight: 1
      default: 1
      options:
        1: PLUGIN_ADMIN.ENABLED
        0: PLUGIN_ADMIN.DISABLED
      validate:
        type: bool
